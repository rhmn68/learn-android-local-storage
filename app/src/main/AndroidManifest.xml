<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rahmanaulia.mynotesapp">

    <permission android:name="com.rahmanaulia.mynotesapp.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="com.rahmanaulia.mynotesapp.WRITE_DATABASE" android:protectionLevel="normal"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name=".provider.NoteProvider"
            android:authorities="com.rahmanaulia.mynotesapp"
            android:exported="true"
            android:readPermission="com.rahmanaulia.mynotesapp.READ_DATABASE"
            android:writePermission="com.rahmanaulia.mynotesapp.WRITE_DATABASE"/>

        <activity android:name=".NoteAddUpdateActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>